# Stage 1 (profiler) dependencies.
# Install with:  pip install -r requirements.txt
# torch is intentionally NOT pinned here: install the build matching the target
# machine's CUDA version first (see scripts/probe_env.py output), then this file.

transformers>=5.0.0
datasets>=3.0.0
accelerate>=1.0.0
safetensors>=0.4.0
numpy>=1.24
pandas>=2.0
pyarrow>=14.0          # parquet writer for the per-token activation trace
matplotlib>=3.7
tqdm>=4.65

# Stage 4 (dashboard) - listed now so the env is built once.
streamlit>=1.30
plotly>=5.18
