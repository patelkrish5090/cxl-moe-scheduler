__pycache__/
*.pyc
.venv/

# Large run artefacts: traces can reach 10^8 rows. Summary CSVs, metadata and
# plots under data/runs/ are small and worth keeping.
data/runs/**/trace.parquet
data/runs/**/trace.csv
data/logs/
data/traces/

CLAUDE.md
docs.md

models/*
models/

# gem5 + DRAMSim3 source and build tree (~10 GB), fetched by
# scripts/build_gem5.sh. The pinned versions are recorded in memsim/README.md;
# the tree itself is never committed.
third_party/

# gem5 run output: stats.txt, config.ini and DRAMSim3's own dumps, one
# directory per simulated point. Regenerate with memsim/run_sweep.py.
memsim/out/
