"""Stage 4 dashboard (docs.md 4.6): renders experiments/'s three-way
comparison and stage 1's activation heatmap. Run the UI with
``streamlit run dashboard/app.py``; the pure data functions it's built on
(dashboard/data.py) are tested offline via ``python -m dashboard.selftest``.
"""
